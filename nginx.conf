server {
	listen 8082;
	server_name erp-view;

	location / {
		root /usr/share/nginx/html;
		try_files $uri $uri/ /index.html;
	}

	location /api/ {
		# /api請求代理到後端
		proxy_pass http://erp-base:8081/;  
	}
}